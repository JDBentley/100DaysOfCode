#Calculate difference in weeks between the input current age and input future age
#Getting inputs
current_age = int(input('What is your current age? '))
target_age = int(input('What is the target age? ')

#Calculating difference
current_weeks = current_age * 52
future_age = target_age * 52
result = current_age - future_age

#Returning result
print(result)